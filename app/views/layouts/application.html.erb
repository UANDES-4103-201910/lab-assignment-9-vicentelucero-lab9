<!DOCTYPE html>
<html>
  <head>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.0.0/jquery.min.js"></script>
    <title>Lab4Assignment</title>
    <script>
      $(document).ready(function(){
         $.ajaxSetup({cache: true});
         $.getScript('https://connect.facebook.net/en_US/sdk.js', function(){
            FB.init({
               appId: '{2320797654834536}',
               version: 'v2.7'
            });
            $('#loginbutton,#feedbutton').removeAttr('disabled');
            FB.getLoginStatus(updateStatusCallback);
         });
      });
    </script>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <style type="text/css">
    .log{
      margin-top: 1px;
      margin-right: 2px;
    }
  </style>

  <body>
    <% if current_user %>
    <button class="log"><%= link_to 'Traidor', log_out_url %></button>
    <label class="log">current_user</label>
    <% else %>
    <button class="log"><%= link_to 'identify yourself', log_in_url %></button>
    <button class="log"><%= link_to 'Join to the Dark Side', registrations_url %></button>
    <% end %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
